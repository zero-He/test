<%@ page pageEncoding="UTF-8"%>
<span>
<locale:message code="que.question.text.queNum"></locale:message>：${que.questionId}
</span>
<span>
(
	<c:if test="${que.source != null}">
		${que.source} ·
	</c:if>
	<c:if test="${que.year != null}">
	${que.year}<locale:message code="que.question.text.year"></locale:message>
	</c:if>
	<c:if test="${que.provinceName != null}">
	${que.provinceName}
	</c:if>
)
</span>