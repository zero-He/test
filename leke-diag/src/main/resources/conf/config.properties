####jdbc####
jdbc.driverClassName=${ci.jdbc.driverClassName}
jdbc.dialectClass=${ci.jdbc.dialectClass}
jdbc.url=${ci.jdbc.url.diag}
jdbc.username=${ci.jdbc.username.diag}
jdbc.password=${ci.jdbc.password.diag}

## beike
jdbc.beike.url=${ci.jdbc.backup.beike.url}
jdbc.beike.username=${ci.jdbc.backup.beike.username}
jdbc.beike.password=${ci.jdbc.backup.beike.password}
## lesson
jdbc.lesson.url=${ci.jdbc.backup.lesson.url}
jdbc.lesson.username=${ci.jdbc.backup.lesson.username}
jdbc.lesson.password=${ci.jdbc.backup.lesson.password}
## homework
jdbc.homework.url=${ci.jdbc.backup.homework.url}
jdbc.homework.password=${ci.jdbc.backup.homework.password}
jdbc.homework.username=${ci.jdbc.backup.homework.username}
## wrongtopic
jdbc.wrongtopic.url=${ci.jdbc.backup.wrongtopic.url}
jdbc.wrongtopic.username=${ci.jdbc.backup.wrongtopic.username}
jdbc.wrongtopic.password=${ci.jdbc.backup.wrongtopic.password}
## note
jdbc.note.url=${ci.jdbc.backup.note.url}
jdbc.note.username=${ci.jdbc.backup.note.username}
jdbc.note.password=${ci.jdbc.backup.note.password}

####cache####
redis.host1=${ci.redis.host1}
redis.port1=${ci.redis.port1}
redis.pool.maxActive=${ci.redis.pool.maxActive}
redis.pool.maxIdle=${ci.redis.pool.maxIdle}
redis.pool.maxWait=${ci.redis.pool.maxWait}

####mongodb####
mongodb.replicaset=${ci.mongo.homework.replicaset}

mongodb.credentials=${ci.mongo.credentials}

mongodb.homework.dbName=${ci.mongo.homework.db}
mongodb.lesson.dbName=${ci.mongo.lesson.db}
mongodb.incentive.dbName=${ci.mongo.incentive.db}
mongodb.beike.dbName=${ci.mongo.beike.db}
mongodb.diag.dbName=${ci.mongo.diag.db}
mongodb.monitor.dbName=${ci.mongo.monitor.db}
mongodb.learn.dbName=${ci.mongo.learn.db}

####webservice####
domain.cas=${ci.domain.cas}
domain.tutor=${ci.domain.tutor}
domain.question=${ci.domain.question}
domain.paper=${ci.domain.paper}
domain.incentive=${ci.domain.incentive}
domain.beike=${ci.domain.beike}

####mq####
mq.host=${ci.mq.host}
mq.port=${ci.mq.port}
mq.username=${ci.mq.username}
mq.password=${ci.mq.password}

### analyze ###
mq.e.homework.analyze=${ci.mq.e.homework.analyze}
mq.q.homework.analyze=${ci.mq.q.homework.analyze}
